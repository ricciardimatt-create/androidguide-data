name: Update device database

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 1 * *"

permissions:
  contents: write

jobs:
  update-devices:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Run pipe (with validation gates)
        run: python update_devices.py

      - name: Commit updated devices.json
        run: |
          git config user.name "androidguide-bot"
          git config user.email "actions@github.com"
          git add devices.json
          git diff --cached --quiet || git commit -m "Update device database"
          git push

      - name: Purge jsDelivr CDN
        run: curl -fsS "https://purge.jsdelivr.net/gh/ricciardimatt-create/androidguide-data@main/devices.json" || true
